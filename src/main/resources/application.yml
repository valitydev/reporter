server:
  port: @server.port@
partyManagement:
  url: http://localhost:8022/repo
magista:
  url: http://localhost:8022/stat
signer:
  url: http://localhost:8022/signer
domainConfig:
  url: http://localhost:8022/domain
spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: org.postgresql.Driver
    url: @db.url@
    username: @db.user@
    password: @db.password@
    hikari:
      maximum-pool-size: 3
      idle-timeout: 30000
storage:
  endpoint: localhost
  signingRegion: RU
  bucketName: "reports"
  client:
    maxErrorRetry: 10

